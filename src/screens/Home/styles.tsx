import { StyleSheet } from "react-native"

const s = StyleSheet.create({
    container: {
        margin: 32
    },

    subtitle: {
        fontSize: 20,
        color: '#000000',
        marginTop: 40,
        marginBottom: 18
    }
})

export default s